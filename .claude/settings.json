{
  "permissions": {
    "allow": [
      "Bash(CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc cargo build --release --target aarch64-unknown-linux-gnu *)",
      "Bash(journalctl -u rustyclaw *)"
    ]
  }
}
