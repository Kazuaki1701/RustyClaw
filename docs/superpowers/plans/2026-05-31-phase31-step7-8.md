# Phase 31 STEP 7–8（運用衛生・信頼性）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 設定/運用衛生（ISSUE-08/11/12/15/16）と信頼性の軽量改善（ISSUE-01検証・02・03）を実装する。

**Architecture:** STEP 7 はコード変更を最小化し、`scripts/` の検証スクリプト・git 衛生・rp1 オプス・LM Studio 運用手順が中心。STEP 8 は `rustyclaw-storage` のトークン圧縮ロジックに固定オーバーヘッド考慮を足し（`cargo test` 検証）、`rustyclaw-gateway` の起動時 GWS カレンダー解決を並列化する。

**Tech Stack:** Rust（rusqlite/chrono/tokio）, bash + jq + curl（検証スクリプト）, git, systemd（rp1）。

**スコープ決定（2026-05-31）:** ISSUE-11=「重複容認＋同期チェック」。STEP 8=「軽量な第一歩のみ」（ISSUE-02/03 の本格版は Phase 24 / 28b に委譲）。ISSUE-01 は既に解消済み（tools は `"type":"string"`＋`parse()`）→ 検証クローズのみ。

**前提コマンド:**
- ビルド: `cargo build -p rustyclaw-gateway`
- テスト: `cargo test -p rustyclaw-storage`
- rp1: `ssh rp1 '...'`、デプロイ `./scripts/deploy.sh`

---

## STEP 7: 設定・運用衛生（ISSUE-08 / 11 / 12 / 15 / 16）

### Task 7.1: config debug/release の model_list 同期チェック（ISSUE-11）

重複は容認し、`model_list` のドリフトを検出するスクリプトを追加する。

**Files:**
- Create: `scripts/check-config-sync.sh`

- [ ] **Step 1: チェックスクリプトを作成**

`scripts/check-config-sync.sh` を新規作成:
```bash
#!/usr/bin/env bash
# config.debug.json と config.release.json の model_list 差分を検出する。
# 差分があれば非ゼロ終了（CI/デプロイ前チェック用）。
set -euo pipefail
DIR="$(cd "$(dirname "$0")/.." && pwd)/production/config"
DEBUG="$DIR/config.debug.json"
RELEASE="$DIR/config.release.json"

for f in "$DEBUG" "$RELEASE"; do
  [ -f "$f" ] || { echo "ERROR: not found: $f"; exit 2; }
done

# model_list を正規化（キー順ソート）して比較
d=$(jq -S '.model_list' "$DEBUG")
r=$(jq -S '.model_list' "$RELEASE")
if [ "$d" == "$r" ]; then
  echo "[OK] model_list is in sync between debug and release."
  exit 0
else
  echo "[DRIFT] model_list differs between config.debug.json and config.release.json:"
  diff <(echo "$d") <(echo "$r") || true
  exit 1
fi
```

- [ ] **Step 2: 実行権限付与＆実行**

Run:
```bash
chmod +x scripts/check-config-sync.sh
./scripts/check-config-sync.sh
```
Expected: `[OK] model_list is in sync ...`（現状 debug/release の model_list は同一）。差分があれば `[DRIFT]` ＋ diff を表示して exit 1。

- [ ] **Step 3: コミット**

```bash
git add scripts/check-config-sync.sh
git commit -m "chore(config): add model_list sync check between debug/release (ISSUE-11)"
```

### Task 7.2: config.json symlink のコミット衛生（ISSUE-12）

アクティブプロファイルの symlink を追跡対象から外し、ローカル選択にする。

**Files:**
- Modify: `.gitignore`
- Untrack: `production/config/config.json`

- [ ] **Step 1: symlink を追跡解除（内容は残す）**

Run:
```bash
git rm --cached production/config/config.json
```
Expected: `rm 'production/config/config.json'`（作業ツリーの symlink 実体は残る）。

- [ ] **Step 2: .gitignore に追加**

`.gitignore` の `/config/` 行の下（cruft セクション付近）に追記:
```
# アクティブ profile の symlink（debug/release のどちらを指すかはローカル運用判断・追跡しない）
/production/config/config.json
```

- [ ] **Step 3: デプロイ手順に profile 設定を明記**

`docs/README.md` のデプロイ節（§4 付近、`deploy.sh` の説明の直後）に追記:
```markdown
> **config profile**: `production/config/config.json` は追跡対象外の symlink。
> 本番は release、デバッグは debug を指すよう運用側で設定する:
> `cd production/config && ln -sfn config.release.json config.json`（本番） / `ln -sfn config.debug.json config.json`（デバッグ）
```

- [ ] **Step 4: 反映確認**

Run: `git status --short production/config/`
Expected: `config.json` が `??` でも `D` でもなく、ignore されて status に出ない。

- [ ] **Step 5: コミット**

```bash
git add .gitignore docs/README.md
git commit -m "chore(config): untrack active config.json symlink to prevent debug-on-prod commits (ISSUE-12)"
```

### Task 7.3: LM Studio ロード済み context 長チェック（ISSUE-08）

config の `context_window` が LM Studio の実ロード長を超えていないか検証するスクリプト。

**Files:**
- Create: `scripts/check-lmstudio-context.sh`

- [ ] **Step 1: スクリプトを作成**

`scripts/check-lmstudio-context.sh` を新規作成（引数: API ベース URL とモデル ID と config 上の context_window）:
```bash
#!/usr/bin/env bash
# LM Studio のロード済み context 長と、RustyClaw config の context_window を比較する。
# 使い方: ./scripts/check-lmstudio-context.sh <base_host:port> <model_id> <config_ctx_tokens>
# 例:      ./scripts/check-lmstudio-context.sh 192.168.1.110:1234 google/gemma-4-e4b 16384
set -euo pipefail
HOSTPORT="${1:?base host:port required}"
MODEL="${2:?model id required}"
CFG_CTX="${3:?config context tokens required}"

json=$(curl -s -m 5 "http://${HOSTPORT}/api/v0/models") || { echo "ERROR: cannot reach LM Studio at ${HOSTPORT}"; exit 2; }
loaded=$(echo "$json" | jq -r --arg m "$MODEL" '.data[]? | select(.id==$m) | .loaded_context_length // empty')
state=$(echo "$json" | jq -r --arg m "$MODEL" '.data[]? | select(.id==$m) | .state // "unknown"')

if [ -z "$loaded" ] || [ "$loaded" == "null" ]; then
  echo "[WARN] model '$MODEL' not loaded (state=$state). Load it in LM Studio first."
  exit 1
fi
echo "model=$MODEL state=$state loaded_context_length=$loaded config_context=$CFG_CTX"
if [ "$CFG_CTX" -gt "$loaded" ]; then
  echo "[MISMATCH] config context_window ($CFG_CTX) > LM Studio loaded ($loaded). Reload model with larger context or lower config."
  exit 1
fi
echo "[OK] config context fits within LM Studio loaded context."
```

- [ ] **Step 2: 実行確認（LM Studio 稼働時）**

Run:
```bash
chmod +x scripts/check-lmstudio-context.sh
./scripts/check-lmstudio-context.sh 192.168.1.110:1234 google/gemma-4-e4b 16384
```
Expected: `[OK] config context fits ...`（16384 ロード時）。未ロードなら `[WARN]`、超過なら `[MISMATCH]`。

- [ ] **Step 3: コミット**

```bash
git add scripts/check-lmstudio-context.sh
git commit -m "chore(ops): add LM Studio loaded-context vs config check (ISSUE-08)"
```

### Task 7.4: rp1 に sqlite3 を導入（ISSUE-15・オプス）

state DB の手動点検を可能にする。

- [ ] **Step 1: rp1 にインストール**

Run: `ssh rp1 'sudo apt-get install -y sqlite3'`
Expected: インストール完了（既存なら "already the newest"）。

- [ ] **Step 2: 動作確認**

Run: `ssh rp1 'sqlite3 ~/.rustyclaw/workspace/memory.db "SELECT COUNT(*) FROM usage;"'`
Expected: usage テーブルの行数が数値で返る。

- [ ] **Step 3: README に記録**

`docs/README.md` のデプロイ節に「rp1 前提パッケージ: `sqlite3`（DB 点検用）」を1行追記し、コミット:
```bash
git add docs/README.md
git commit -m "docs(ops): note sqlite3 prerequisite on rp1 for DB inspection (ISSUE-15)"
```

### Task 7.5: LM Studio 埋め込みモデルの常駐化（ISSUE-16・オプス/手順）

`memory_search` 初回の JIT ロード遅延/失敗を避ける。

- [ ] **Step 1: LM Studio 側設定**

LM Studio の `text-embedding-nomic-embed-text-v1.5` を手動ロードし、設定で「Keep model loaded in memory」（JIT アンロード無効）を有効化する。

- [ ] **Step 2: ロード確認**

Run: `./scripts/check-lmstudio-context.sh 192.168.1.110:1234 text-embedding-nomic-embed-text-v1.5 2048`
Expected: `state=loaded`（`[WARN] not loaded` が出ないこと）。

- [ ] **Step 3: 運用メモを README に追記してコミット**

`docs/README.md` に「LM Studio 利用時は埋め込みモデルを常駐ロードする（memory_search 初回遅延回避）」を追記:
```bash
git add docs/README.md
git commit -m "docs(ops): keep LM Studio embedding model resident for memory_search (ISSUE-16)"
```

---

## STEP 8: 信頼性・安定性（ISSUE-01 検証 / 02 / 03）

### Task 8.1: ISSUE-01（数値パラメータ型）の検証クローズ

- [ ] **Step 1: 現状コードの確認**

Run: `grep -n '"type": *"string"' crates/rustyclaw-tools/src/lib.rs`
Expected: `limit`/`query` 等が `"type":"string"` で定義され、`Value::String(s) => s.parse::<u64>()` で安全パースしていること（`crates/rustyclaw-tools/src/lib.rs:114,121,394,405` 付近）。

- [ ] **Step 2: テストで回帰防止を確認**

Run: `cargo test -p rustyclaw-tools`
Expected: 既存テスト PASS（文字列 limit のパースを含む）。

- [ ] **Step 3: task.md / 改善計画にクローズを記録**

`docs/rusty_claw_improvement_plan.md` の ISSUE-01 行と `docs/task.md` Phase 31 STEP 8 の ISSUE-01 を「✅ 解消済み（Phase 20 で対処・本STEPで検証）」に更新し、コミット:
```bash
git add docs/rusty_claw_improvement_plan.md docs/task.md
git commit -m "docs(phase31): close ISSUE-01 (string-typed numeric params verified)"
```

### Task 8.2: トークン圧縮に system+tools オーバーヘッドを考慮（ISSUE-02 軽量版）

**問題:** `estimate_tokens()` はメッセージ本文のみ計上し、システムプロンプト＋ツール定義（≒4,700トークン）を無視 → `execute_with_tools` で 413（`n_keep` 超過）。`compact_if_needed` をオーバーヘッド考慮版にする。

**Files:**
- Modify: `crates/rustyclaw-storage/src/lib.rs`（`compact_if_needed` の下に新メソッド追加）
- Test: `crates/rustyclaw-storage/src/lib.rs`（`mod tests`）
- Modify: `crates/rustyclaw-agent/src/lib.rs:769`（`execute_with_tools` の圧縮呼び出し）

- [ ] **Step 1: 失敗するテストを追加**

`crates/rustyclaw-storage/src/lib.rs` の `mod tests` 内に追加:
```rust
    #[test]
    fn overhead_lowers_effective_limit_and_triggers_compaction() {
        // 10 件 × 100 文字 ≒ 1,500 推定トークン
        let msgs: Vec<Message> = (0..10)
            .map(|_| Message { role: "user".into(), content: "a".repeat(100), name: None, ..Default::default() })
            .collect();
        // overhead=0・大きな上限 → 圧縮されない
        let mut h0 = ConversationHistory::new(msgs.clone());
        assert!(!h0.compact_if_needed_with_overhead(10_000, 0));
        // overhead 大で実効上限が下がり圧縮が発火する
        let mut h1 = ConversationHistory::new(msgs.clone());
        assert!(h1.compact_if_needed_with_overhead(2_000, 1_800));
    }
```

- [ ] **Step 2: テストが失敗（未定義メソッド）することを確認**

Run: `cargo test -p rustyclaw-storage overhead_lowers_effective_limit`
Expected: コンパイルエラー（`compact_if_needed_with_overhead` 未定義）。

- [ ] **Step 3: メソッドを実装**

`crates/rustyclaw-storage/src/lib.rs` の `compact_if_needed` 関数の直後に追加:
```rust
    /// system プロンプト＋ツール定義などの固定オーバーヘッド（推定トークン）を考慮して圧縮する。
    /// 実効上限を `limit - overhead_tokens` に下げてから既存の圧縮判定を行うことで、
    /// 履歴＋システム＋ツールの合計がモデル上限を超えて 413 になるのを防ぐ。
    pub fn compact_if_needed_with_overhead(&mut self, limit: usize, overhead_tokens: usize) -> bool {
        let effective = limit.saturating_sub(overhead_tokens);
        self.compact_if_needed(effective)
    }
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cargo test -p rustyclaw-storage`
Expected: 全テスト PASS。

- [ ] **Step 5: `execute_with_tools` の呼び出しを差し替え**

`crates/rustyclaw-agent/src/lib.rs:768-769` の現状:
```rust
        let history_limit = self.get_history_limit(purpose);
        history.compact_if_needed(history_limit);
```
を次に置換（`system_context` とツールスキーマからオーバーヘッドを推定。`system_context` は同関数内で既に構築済み、`tool_registry` は引数）:
```rust
        let history_limit = self.get_history_limit(purpose);
        // ISSUE-02: system プロンプト＋ツール定義分のトークンを考慮して実効上限を下げる
        let overhead_chars = system_context.chars().count()
            + tool_registry.to_llm_schemas().iter().map(|s| s.to_string().chars().count()).sum::<usize>();
        let overhead_tokens = (overhead_chars * 3) / 2;
        history.compact_if_needed_with_overhead(history_limit, overhead_tokens);
```

- [ ] **Step 6: ビルド＆テスト**

Run: `cargo build -p rustyclaw-agent && cargo test -p rustyclaw-storage`
Expected: コンパイル成功・テスト PASS。

- [ ] **Step 7: コミット**

```bash
git add crates/rustyclaw-storage/src/lib.rs crates/rustyclaw-agent/src/lib.rs
git commit -m "fix(agent): account for system+tools overhead in history compaction (ISSUE-02)"
```

### Task 8.3: 起動時の GWS カレンダー解決を並列化（ISSUE-03 軽量版）

**問題:** `Gateway::run` の書き込み可能カレンダー解決が `gws` を**逐次 await** しており、カレンダー数×0.4秒ほど起動を遅延させる。並列化して短縮する（深い非同期初期化リファクタは Phase 28b-2 に委譲）。

**Files:**
- Modify: `crates/rustyclaw-gateway/src/lib.rs:725-748`（解決ループ）

- [ ] **Step 1: 逐次ループを並列 spawn に置換**

`crates/rustyclaw-gateway/src/lib.rs:726-748`（`let mut writable_calendars...` の宣言から for ループ終わりまで）の現状:
```rust
                    let mut writable_calendars: Vec<(String, String, String)> = Vec::new();
                    for cal_id in &gws.writable_calendar_ids {
                        let params = serde_json::json!({ "calendarId": cal_id }).to_string();
                        match tokio::process::Command::new(&gws_path)
                            .args(["calendar", "calendarList", "get", "--params", &params, "--format", "json"])
                            .output()
                            .await
                        {
                            Ok(out) if out.status.success() => {
                                let body = String::from_utf8_lossy(&out.stdout);
                                if let Ok(d) = serde_json::from_str::<serde_json::Value>(&body) {
                                    let name = d["summary"].as_str().unwrap_or(cal_id).to_string();
                                    let desc = d["description"].as_str().unwrap_or("").to_string();
                                    tracing::info!("Calendar resolved: '{}' ({})", name, cal_id);
                                    writable_calendars.push((cal_id.clone(), name, desc));
                                }
                            }
                            _ => {
                                tracing::warn!("Failed to fetch calendar info for {}. Using ID as name.", cal_id);
                                writable_calendars.push((cal_id.clone(), cal_id.clone(), String::new()));
                            }
                        }
                    }
```
を次に置換（各カレンダーを `tokio::spawn` で並列実行し、順に await して集約）:
```rust
                    // ISSUE-03: カレンダー解決を並列化して起動ブロッキングを短縮する
                    let mut handles = Vec::new();
                    for cal_id in gws.writable_calendar_ids.clone() {
                        let gws_path = gws_path.clone();
                        handles.push(tokio::spawn(async move {
                            let params = serde_json::json!({ "calendarId": &cal_id }).to_string();
                            match tokio::process::Command::new(&gws_path)
                                .args(["calendar", "calendarList", "get", "--params", &params, "--format", "json"])
                                .output()
                                .await
                            {
                                Ok(out) if out.status.success() => {
                                    let body = String::from_utf8_lossy(&out.stdout);
                                    if let Ok(d) = serde_json::from_str::<serde_json::Value>(&body) {
                                        let name = d["summary"].as_str().unwrap_or(&cal_id).to_string();
                                        let desc = d["description"].as_str().unwrap_or("").to_string();
                                        tracing::info!("Calendar resolved: '{}' ({})", name, cal_id);
                                        return (cal_id.clone(), name, desc);
                                    }
                                    (cal_id.clone(), cal_id.clone(), String::new())
                                }
                                _ => {
                                    tracing::warn!("Failed to fetch calendar info for {}. Using ID as name.", cal_id);
                                    (cal_id.clone(), cal_id.clone(), String::new())
                                }
                            }
                        }));
                    }
                    let mut writable_calendars: Vec<(String, String, String)> = Vec::new();
                    for h in handles {
                        if let Ok(tuple) = h.await {
                            writable_calendars.push(tuple);
                        }
                    }
```

- [ ] **Step 2: ビルド**

Run: `cargo build -p rustyclaw-gateway`
Expected: コンパイル成功。

- [ ] **Step 3: 手動確認（起動ログ）**

ローカル起動（gws 有効 config）またはデプロイ後に rp1 起動ログを確認:
```bash
ssh rp1 'grep -E "Calendar resolved|Registering gws writable" ~/.rustyclaw/logs/rustyclaw.log.$(date +%F)'
```
Expected: 2件の "Calendar resolved" が（ほぼ同時刻に）出力され、`Registering gws writable calendar tool (2 calendars).` が続く。機能退行なし。

- [ ] **Step 4: コミット**

```bash
git add crates/rustyclaw-gateway/src/lib.rs
git commit -m "perf(gateway): parallelize gws calendar resolution at startup (ISSUE-03)"
```

---

## 完了確認（全 STEP 後）

- [ ] `cargo build` 成功・`cargo test -p rustyclaw-storage` / `-p rustyclaw-tools` 全 PASS
- [ ] `./scripts/check-config-sync.sh` → `[OK]`
- [ ] `./scripts/check-lmstudio-context.sh 192.168.1.110:1234 google/gemma-4-e4b 16384` → `[OK]`
- [ ] `git status` で `production/config/config.json` が ignore され表示されない
- [ ] `ssh rp1 'which sqlite3'` がパスを返す
- [ ] デプロイ後、起動ログのカレンダー解決が並列化され、413 エラーが再発しない（長い対話で確認）

## スコープ外（委譲）
- ISSUE-02/03 の本格版（estimate_tokens の全面再設計・モデル別 TPM 連動の厳密予算・初期化フローの完全非同期化）は **Phase 24（耐障害性）/ Phase 28b（起動最適化）** に委譲。
- ISSUE-11 の base+overlay ローダー化は将来検討（今回は同期チェックのみ）。
