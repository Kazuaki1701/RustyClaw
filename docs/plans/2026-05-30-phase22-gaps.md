# Phase 22: GeminiClaw 移植ギャップ回収 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** heartbeat-digest の汚染修正・Proactive Posts の永続化・MemorySearchTool / ObsidianWriteTool の追加という 3 つの独立した改善を実施する。

**Architecture:** Task 1 は heartbeat.rs のスキャンフィルタ 1 行修正。Task 2 は heartbeat.rs に書き込み・agent/lib.rs に読み込みを追加（新ファイル `memory/proactive-posts.md`）。Task 3 は rustyclaw-tools に 2 ツール追加し Gateway で登録する。3 タスクは互いに依存しないため任意の順で実行できる。

**Tech Stack:** Rust / tokio / reqwest（既存）/ rustyclaw-storage::SearchIndexManager（既存）

---

## ファイルマップ

| ファイル | 変更種別 | Task |
|---|---|---|
| `crates/rustyclaw-gateway/src/heartbeat.rs` | 修正（フィルタ追加 + proactive 書き込み） | 1・2 |
| `crates/rustyclaw-agent/src/lib.rs` | 修正（build_system_context に proactive 読み込み） | 2 |
| `crates/rustyclaw-tools/src/lib.rs` | 修正（MemorySearchTool・ObsidianWriteTool 追加） | 3 |
| `crates/rustyclaw-tools/Cargo.toml` | 修正（rustyclaw-storage 依存追加） | 3 |
| `crates/rustyclaw-gateway/src/lib.rs` | 修正（2 ツールの登録） | 3 |

---

## Task 1: heartbeat-digest の汚染セッション除外

**Files:**
- Modify: `crates/rustyclaw-gateway/src/heartbeat.rs`

> **問題**: `generate_digest()` のスキャンが `cli-session.jsonl`・`http-dashboard.jsonl` を除外していないため、CLI テストの `[NO-AGENT]` エントリでダイジェストが汚染される。修正は 1 行。

- [ ] **Step 1: 失敗テストを書く**

`crates/rustyclaw-gateway/src/heartbeat.rs` の `#[cfg(test)]` ブロック末尾に追加:

```rust
#[test]
fn test_generate_digest_excludes_cli_and_http_sessions() -> Result<()> {
    let dir = tempfile::tempdir()?;
    let ws = dir.path();
    let sessions_dir = ws.join("sessions");
    let memory_dir = ws.join("memory");
    std::fs::create_dir_all(&sessions_dir)?;
    std::fs::create_dir_all(&memory_dir)?;

    // CLI テストセッションと HTTP ダッシュボードセッションを作成
    std::fs::write(
        sessions_dir.join("cli-session.jsonl"),
        "{\"role\":\"user\",\"content\":\"test\"}\n{\"role\":\"assistant\",\"content\":\"[NO-AGENT] Debug mode\"}\n",
    )?;
    std::fs::write(
        sessions_dir.join("http-dashboard.jsonl"),
        "{\"role\":\"user\",\"content\":\"ping\"}\n{\"role\":\"assistant\",\"content\":\"pong\"}\n",
    )?;
    // 正規の Discord セッション
    std::fs::write(
        sessions_dir.join("discord-C123-20260530.jsonl"),
        "{\"role\":\"user\",\"content\":\"こんにちは\"}\n{\"role\":\"assistant\",\"content\":\"やあ！\"}\n",
    )?;

    let db_path = memory_dir.join("memory.db");
    let db = rustyclaw_storage::DbManager::new(&db_path)?;
    let bus = std::sync::Arc::new(crate::MessageBus::new());
    let config = rustyclaw_config::Config::default();
    let svc = HeartbeatService::new(config, ws.to_path_buf(), bus);

    let digest = svc.generate_digest(&db)?;

    assert!(!digest.contains("NO-AGENT"), "cli-session エントリが除外されるべき");
    assert!(!digest.contains("ping"), "http-dashboard エントリが除外されるべき");
    assert!(digest.contains("こんにちは") || digest.contains("やあ"), "Discord セッションは含まれるべき");
    Ok(())
}
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
cargo test -p rustyclaw-gateway test_generate_digest_excludes 2>&1 | tail -10
```
Expected: テストが通過してしまう（バグが存在するため）か、assert! に失敗する。

- [ ] **Step 3: フィルタを修正する**

`crates/rustyclaw-gateway/src/heartbeat.rs` の `generate_digest` 内（約 82 行目）の既存コード:

```rust
                if !filename.ends_with(".jsonl") || filename.starts_with("cron") {
                    continue; // 自己参照 (cron-*) 除外
                }
```

を以下に変更:

```rust
                if !filename.ends_with(".jsonl")
                    || filename.starts_with("cron")
                    || filename.starts_with("cli-")
                    || filename.starts_with("http-")
                {
                    continue; // cron-* / CLI テスト / HTTP ダッシュボード除外
                }
```

- [ ] **Step 4: テストが通ることを確認**

```bash
cargo test -p rustyclaw-gateway test_generate_digest_excludes 2>&1 | tail -5
```
Expected: `test result: ok. 1 passed`

全テスト確認:
```bash
cargo test -p rustyclaw-gateway 2>&1 | grep "test result"
```

- [ ] **Step 5: コミット**

```bash
git add crates/rustyclaw-gateway/src/heartbeat.rs
git commit -m "fix(heartbeat): exclude cli-session and http-dashboard from digest scan"
```

---

## Task 2: Proactive Posts の永続化と ContextBuilder への注入

**Files:**
- Modify: `crates/rustyclaw-gateway/src/heartbeat.rs`（proactive 送信時に追記）
- Modify: `crates/rustyclaw-agent/src/lib.rs`（`build_system_context` で読み込み）

> **問題**: Heartbeat が proactive 投稿をした後、ユーザーが翌日（新 session ID）で会話を開始すると、Heartbeat の発言が履歴に含まれず「自分が言ったこと」を忘れる。  
> **解決策**: proactive 送信時に `memory/proactive-posts.md` へ追記（最新 5 件・48h 以内のみ保持）し、`build_system_context()` でこのファイルを読み込んでシステムプロンプト末尾に注入する。

### 2a. heartbeat.rs への書き込み追加

- [ ] **Step 1: 失敗テストを書く**

`crates/rustyclaw-gateway/src/heartbeat.rs` の `#[cfg(test)]` ブロック末尾に追加:

```rust
#[test]
fn test_proactive_post_writes_to_file() -> Result<()> {
    let dir = tempfile::tempdir()?;
    let ws = dir.path();
    let memory_dir = ws.join("memory");
    std::fs::create_dir_all(ws.join("sessions"))?;
    std::fs::create_dir_all(&memory_dir)?;

    let db_path = memory_dir.join("memory.db");
    let db = rustyclaw_storage::DbManager::new(&db_path)?;
    let bus = std::sync::Arc::new(crate::MessageBus::new());
    let mut config = rustyclaw_config::Config::default();
    // home_channel_id を設定してフォールバック処理を回避
    if let Some(ref mut discord) = config.channels.discord {
        discord.home_channel_id = Some("TEST_CHANNEL".to_string());
    }
    let svc = HeartbeatService::new(config, ws.to_path_buf(), bus);

    // proactive（非 HEARTBEAT_OK）応答を処理
    svc.process_heartbeat_response("雨が近づいています。傘をお持ちください。", &db)?;

    let posts_file = memory_dir.join("proactive-posts.md");
    assert!(posts_file.exists(), "proactive-posts.md が作成されるべき");
    let content = std::fs::read_to_string(&posts_file)?;
    assert!(content.contains("雨が近づいています"), "投稿内容が記録されるべき");
    Ok(())
}
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
cargo test -p rustyclaw-gateway test_proactive_post_writes 2>&1 | tail -10
```
Expected: `FAILED` — `proactive-posts.md` が存在しないため

- [ ] **Step 3: process_heartbeat_response の proactive ブランチに書き込みを追加**

`crates/rustyclaw-gateway/src/heartbeat.rs` の `process_heartbeat_response` のコメント `// 1. Proactive Post 注入 (ユーザーの会話履歴ファイルへ記録)` のブロック（logger.append_message の前）に以下を追加:

```rust
            // proactive-posts.md への追記（最新 5 件・48h 以内を保持）
            let posts_path = self.workspace_path.join("memory").join("proactive-posts.md");
            let now_label = Local::now().format("%Y-%m-%dT%H:%M:%S%:z").to_string();
            let new_entry = format!("[{}] {}", now_label, response_content.replace('\n', " "));
            let cutoff = Local::now() - chrono::Duration::hours(48);
            let mut kept: Vec<String> = Vec::new();
            if let Ok(existing) = fs::read_to_string(&posts_path) {
                for line in existing.lines() {
                    // "[2026-05-30T..." 形式の行を 48h 以内かチェック
                    if line.starts_with('[') {
                        if let Some(end) = line.find(']') {
                            if let Ok(dt) = chrono::DateTime::parse_from_rfc3339(&line[1..end]) {
                                if dt.with_timezone(&Local) >= cutoff {
                                    kept.push(line.to_string());
                                }
                            }
                        }
                    }
                }
            }
            kept.push(new_entry);
            // 最新 5 件のみ保持
            if kept.len() > 5 {
                kept = kept.split_off(kept.len() - 5);
            }
            let _ = fs::create_dir_all(posts_path.parent().unwrap());
            let _ = rustyclaw_storage::atomic_write(&posts_path, kept.join("\n").as_bytes());
```

- [ ] **Step 4: テストが通ることを確認**

```bash
cargo test -p rustyclaw-gateway test_proactive_post_writes 2>&1 | tail -5
```
Expected: `test result: ok. 1 passed`

- [ ] **Step 5: コミット**

```bash
git add crates/rustyclaw-gateway/src/heartbeat.rs
git commit -m "feat(heartbeat): persist proactive posts to memory/proactive-posts.md"
```

### 2b. build_system_context への読み込み追加

- [ ] **Step 6: 失敗テストを書く**

`crates/rustyclaw-agent/src/lib.rs` の `#[cfg(test)]` ブロック末尾に追加:

```rust
#[test]
fn test_build_system_context_injects_proactive_posts() {
    let dir = tempfile::TempDir::new().unwrap();
    let ws = dir.path();
    let memory_dir = ws.join("memory");
    std::fs::create_dir_all(&memory_dir).unwrap();
    // 標準ファイルを作成
    for f in &["SOUL.md", "AGENTS.md", "MEMORY.md", "USER.md"] {
        std::fs::write(ws.join(f), "").unwrap();
    }
    // proactive-posts.md を作成
    let now = chrono::Local::now().format("%Y-%m-%dT%H:%M:%S%:z").to_string();
    std::fs::write(
        memory_dir.join("proactive-posts.md"),
        format!("[{}] 傘を持ってください。", now),
    ).unwrap();

    let config = rustyclaw_config::Config::default();
    let provider = std::sync::Arc::new(rustyclaw_providers::NoopProvider::new());
    let sem = std::sync::Arc::new(tokio::sync::Semaphore::new(1));
    let pipeline = Pipeline::new(config, sem);
    let ctx = pipeline.build_system_context(ws).unwrap();
    assert!(ctx.contains("傘を持ってください"), "proactive-posts が注入されるべき");
}
```

- [ ] **Step 7: テストが失敗することを確認**

```bash
cargo test -p rustyclaw-agent test_build_system_context_injects_proactive 2>&1 | tail -10
```
Expected: `FAILED` — `proactive-posts` が ctx に含まれていないため

- [ ] **Step 8: build_system_context に読み込みを追加**

`crates/rustyclaw-agent/src/lib.rs` の `build_system_context` 関数の末尾 `Ok(context)` の直前に追加:

```rust
        // proactive-posts.md を注入（存在する場合のみ）
        let posts_path = workspace_dir.join("memory").join("proactive-posts.md");
        if let Ok(posts) = fs::read_to_string(&posts_path) {
            let trimmed = posts.trim();
            if !trimmed.is_empty() {
                context.push_str(&format!(
                    "# Recent AI Proactive Posts\n\n{}\n\n",
                    trimmed
                ));
            }
        }
```

- [ ] **Step 9: テストが通ることを確認**

```bash
cargo test -p rustyclaw-agent test_build_system_context_injects_proactive 2>&1 | tail -5
```
Expected: `test result: ok. 1 passed`

全テスト確認:
```bash
cargo test 2>&1 | grep "test result" | grep -v "0 passed"
```

- [ ] **Step 10: コミット**

```bash
git add crates/rustyclaw-agent/src/lib.rs
git commit -m "feat(agent): inject proactive-posts.md into build_system_context"
```

---

## Task 3: MemorySearchTool + ObsidianWriteTool の追加

**Files:**
- Modify: `crates/rustyclaw-tools/src/lib.rs`
- Modify: `crates/rustyclaw-tools/Cargo.toml`（rustyclaw-storage 依存追加）
- Modify: `crates/rustyclaw-gateway/src/lib.rs`（2 ツールの登録）

### 3a. rustyclaw-storage を依存に追加

- [ ] **Step 1: Cargo.toml を修正**

`crates/rustyclaw-tools/Cargo.toml` の `[dependencies]` に追加:

```toml
rustyclaw-storage = { path = "../rustyclaw-storage" }
```

確認:
```bash
cargo check -p rustyclaw-tools 2>&1 | tail -5
```

### 3b. MemorySearchTool を実装

- [ ] **Step 2: 失敗テストを書く**

`crates/rustyclaw-tools/src/lib.rs` の `#[cfg(test)]` ブロック末尾に追加:

```rust
#[tokio::test]
async fn test_memory_search_tool_schema() {
    let dir = tempfile::tempdir().unwrap();
    let tool = MemorySearchTool::new(dir.path().to_path_buf());
    assert_eq!(tool.name(), "memory_search");
    let params = tool.parameters();
    assert!(params["properties"]["query"].is_object());
    assert_eq!(params["required"][0], "query");
}

#[tokio::test]
async fn test_memory_search_missing_query() {
    let dir = tempfile::tempdir().unwrap();
    let tool = MemorySearchTool::new(dir.path().to_path_buf());
    let res = tool.execute(serde_json::json!({})).await;
    assert!(res.is_error);
    assert!(res.content.contains("Missing"));
}

#[tokio::test]
async fn test_memory_search_empty_index() {
    let dir = tempfile::tempdir().unwrap();
    let tool = MemorySearchTool::new(dir.path().to_path_buf());
    // インデックスが存在しない場合は graceful に処理
    let res = tool.execute(serde_json::json!({"query": "rust"})).await;
    // エラーか「No results」のどちらかを返す（クラッシュしないこと）
    assert!(!res.content.is_empty());
}
```

- [ ] **Step 3: テストが失敗することを確認**

```bash
cargo test -p rustyclaw-tools test_memory_search 2>&1 | tail -5
```

- [ ] **Step 4: MemorySearchTool を実装**

`WorkspaceWriteTool` の後（`WebSearchTool` の前）に追加:

```rust
// ─── MemorySearchTool ────────────────────────────────────────────────────────

pub struct MemorySearchTool {
    workspace_path: std::path::PathBuf,
}

impl MemorySearchTool {
    pub fn new(workspace_path: std::path::PathBuf) -> Self {
        Self { workspace_path }
    }
}

#[async_trait]
impl Tool for MemorySearchTool {
    fn name(&self) -> &str { "memory_search" }

    fn description(&self) -> &str {
        "Search the agent's long-term memory (session summaries) by keyword. Returns file paths of matching summaries."
    }

    fn parameters(&self) -> Value {
        serde_json::json!({
            "type": "object",
            "properties": {
                "query": { "type": "string", "description": "Search query" }
            },
            "required": ["query"]
        })
    }

    async fn execute(&self, args: Value) -> ToolResult {
        let query = match args["query"].as_str() {
            Some(q) => q.to_string(),
            None => return ToolResult { content: "Missing query".into(), is_error: true },
        };
        let index_dir = self.workspace_path.join("memory").join("index");
        match rustyclaw_storage::SearchIndexManager::new(&index_dir) {
            Ok(mgr) => match mgr.search(&query) {
                Ok(paths) if paths.is_empty() => {
                    ToolResult { content: "No results found.".into(), is_error: false }
                }
                Ok(paths) => {
                    let result = paths.iter()
                        .map(|p| p.display().to_string())
                        .collect::<Vec<_>>()
                        .join("\n");
                    ToolResult { content: result, is_error: false }
                }
                Err(e) => ToolResult { content: format!("Search failed: {}", e), is_error: true },
            },
            Err(e) => ToolResult {
                content: format!("Memory index not available (may not have summaries yet): {}", e),
                is_error: true,
            },
        }
    }
}
```

- [ ] **Step 5: テストが通ることを確認**

```bash
cargo test -p rustyclaw-tools test_memory_search 2>&1 | tail -5
```
Expected: `test result: ok. 3 passed`

### 3c. ObsidianWriteTool を実装

- [ ] **Step 6: 失敗テストを書く**

テストブロック末尾に追加:

```rust
#[tokio::test]
async fn test_obsidian_write_tool_schema() {
    let tool = ObsidianWriteTool::new("192.168.1.2".to_string(), "key".to_string());
    assert_eq!(tool.name(), "obsidian_write_note");
    let params = tool.parameters();
    assert!(params["properties"]["path"].is_object());
    assert!(params["properties"]["content"].is_object());
    assert_eq!(params["required"][0].as_str().unwrap(), "path");
    assert_eq!(params["required"][1].as_str().unwrap(), "content");
}

#[tokio::test]
async fn test_obsidian_write_missing_path() {
    let tool = ObsidianWriteTool::new("192.168.1.2".to_string(), "key".to_string());
    let res = tool.execute(serde_json::json!({"content": "hello"})).await;
    assert!(res.is_error);
    assert!(res.content.contains("Missing path"));
}

#[tokio::test]
async fn test_obsidian_write_missing_content() {
    let tool = ObsidianWriteTool::new("192.168.1.2".to_string(), "key".to_string());
    let res = tool.execute(serde_json::json!({"path": "Test/Note.md"})).await;
    assert!(res.is_error);
    assert!(res.content.contains("Missing content"));
}
```

- [ ] **Step 7: テストが失敗することを確認**

```bash
cargo test -p rustyclaw-tools test_obsidian_write 2>&1 | tail -5
```

- [ ] **Step 8: ObsidianWriteTool を実装**

`ObsidianReadTool` の後に追加:

```rust
// ─── ObsidianWriteTool ───────────────────────────────────────────────────────

pub struct ObsidianWriteTool {
    host: String,
    api_key: String,
}

impl ObsidianWriteTool {
    pub fn new(host: String, api_key: String) -> Self {
        Self { host, api_key }
    }
}

#[async_trait]
impl Tool for ObsidianWriteTool {
    fn name(&self) -> &str { "obsidian_write_note" }

    fn description(&self) -> &str {
        "Create or update a note in Obsidian vault. Use mode 'append' to add content to an existing note, or 'write' to overwrite."
    }

    fn parameters(&self) -> Value {
        serde_json::json!({
            "type": "object",
            "properties": {
                "path":    { "type": "string", "description": "Vault-relative path, e.g. 'Folder/Note.md'" },
                "content": { "type": "string", "description": "Markdown content to write" },
                "mode":    { "type": "string", "description": "\"write\" (overwrite, default) or \"append\"" }
            },
            "required": ["path", "content"]
        })
    }

    async fn execute(&self, args: Value) -> ToolResult {
        let path = match args["path"].as_str() {
            Some(p) => p.to_string(),
            None => return ToolResult { content: "Missing path".into(), is_error: true },
        };
        let new_content = match args["content"].as_str() {
            Some(c) => c.to_string(),
            None => return ToolResult { content: "Missing content".into(), is_error: true },
        };
        let mode = args["mode"].as_str().unwrap_or("write");
        let client = reqwest::Client::new();
        let url = format!("http://{}:27123/vault/{}", self.host, percent_encode(&path));
        let auth = format!("Bearer {}", self.api_key);

        let body = if mode == "append" {
            // GET 既存コンテンツ → 末尾に追記
            match client.get(&url).header("Authorization", &auth).send().await {
                Ok(resp) if resp.status().is_success() => {
                    let existing = resp.text().await.unwrap_or_default();
                    format!("{}\n{}", existing.trim_end(), new_content)
                }
                _ => new_content.clone(), // ファイル未存在は新規作成
            }
        } else {
            new_content
        };

        match client
            .put(&url)
            .header("Authorization", &auth)
            .header("Content-Type", "text/markdown")
            .body(body)
            .send()
            .await
        {
            Ok(resp) if resp.status().is_success() || resp.status().as_u16() == 204 => {
                ToolResult { content: format!("Written to {}", path), is_error: false }
            }
            Ok(resp) => ToolResult {
                content: format!("Obsidian write error: HTTP {}", resp.status()),
                is_error: true,
            },
            Err(e) => ToolResult { content: format!("Obsidian write failed: {}", e), is_error: true },
        }
    }
}
```

- [ ] **Step 9: テストが通ることを確認**

```bash
cargo test -p rustyclaw-tools 2>&1 | grep "test result"
```
Expected: 全テスト通過

- [ ] **Step 10: コミット**

```bash
git add crates/rustyclaw-tools/src/lib.rs crates/rustyclaw-tools/Cargo.toml
git commit -m "feat(tools): add MemorySearchTool and ObsidianWriteTool"
```

### 3d. Gateway へのツール登録

- [ ] **Step 11: gateway/src/lib.rs に登録コードを追加**

Obsidian ツール登録ブロック（`tracing::info!("Registered native Obsidian tools.")` の直前）を探し、`ObsidianReadTool` の登録行の直後に追加:

```rust
                tool_registry.register(Arc::new(rustyclaw_tools::ObsidianWriteTool::new(
                    o.host.clone(), o.api_key.clone(),
                )));
```

次に、Workspace I/O ツール登録ブロック（`tracing::info!("Registered Workspace I/O tools.")` の直前）の後に追加:

```rust
        // MemorySearchTool 登録（workspace_path ベース）
        tool_registry.register(Arc::new(rustyclaw_tools::MemorySearchTool::new(self.workspace_path.clone())));
        tracing::info!("Registered MemorySearchTool (tantivy).");
```

- [ ] **Step 12: cargo check が通ることを確認**

```bash
cargo check -p rustyclaw-gateway 2>&1 | grep "^error" | head -5
```

- [ ] **Step 13: 全テスト通過を確認**

```bash
cargo test 2>&1 | grep "test result" | grep -v "0 passed"
```
Expected: 全テスト通過（0 failed）

- [ ] **Step 14: コミット**

```bash
git add crates/rustyclaw-gateway/src/lib.rs
git commit -m "feat(gateway): register MemorySearchTool and ObsidianWriteTool"
```

---

## Self-Review

### Spec Coverage チェック

| 要件 | 対応タスク |
|---|---|
| 22-2: cli-session / http-dashboard をダイジェストから除外 | Task 1 ✅ |
| 22-1: proactive 送信時に proactive-posts.md へ書き込み | Task 2a ✅ |
| 22-1: build_system_context で proactive-posts.md を読み込み | Task 2b ✅ |
| 22-1: 48h 超エントリの自動プルーニング | Task 2a ✅ |
| 22-1: 最新 5 件に制限 | Task 2a ✅ |
| 22-3: MemorySearchTool（tantivy 検索） | Task 3b ✅ |
| 22-3: ObsidianWriteTool（write / append） | Task 3c ✅ |
| 22-3: Gateway 登録 | Task 3d ✅ |

### Placeholder チェック

- 「TBD」「TODO」なし ✅
- 全コードブロックに実装済みコードあり ✅
- テストに具体的なアサーションあり ✅

### 型整合性チェック

- `MemorySearchTool::new(PathBuf)` → 登録時 `self.workspace_path.clone()` ✅
- `ObsidianWriteTool::new(String, String)` → `o.host.clone(), o.api_key.clone()` ✅
- `rustyclaw_storage::SearchIndexManager` は `rustyclaw-storage` 依存追加後に解決 ✅
